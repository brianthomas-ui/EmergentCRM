node_modules/
__pycache__/
*.pyc
backend/.venv/
.venv/
*.egg-info/
.DS_Store
backend/.env
frontend/.env
docker-compose.draft.yml
# Production secrets + origin certs (never commit)
.env.prod
.env.prod.local
deploy/certs/*
!deploy/certs/.gitkeep
# NOTE: tools/ (the emergentintegrations shim) IS committed now — the self-host backend
# build COPYs it. It is a local drop-in and never re-imported to the Emergent platform.
memory/test_credentials.md
.env
.env.*
*.env
credentials.json
*.pem
*.key
.credentials
