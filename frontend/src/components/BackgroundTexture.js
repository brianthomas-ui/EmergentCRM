// Deprecated: the animated monospace texture caused screenshot/build timeouts
// (handoff §1 "Known caution"). The Emergent Dark Sales Console uses a flat
// near-black background instead. Kept as a no-op so existing imports don't break.
export default function BackgroundTexture() {
  return null;
}
